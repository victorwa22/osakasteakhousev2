# Netlify config: build and deploy the React app in osaka-new/

[build]
  base    = "osaka-new"
  command = "npm run build"
  publish = "dist"

# SPA fallback (for future client-side routing)
[[redirects]]
  from = "/*"
  to   = "/index.html"
  status = 200
